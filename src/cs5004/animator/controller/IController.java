package cs5004.animator.controller;

/**
 * This is the interface for the controller. It supports method needed to animate the controller.
 */
public interface IController {

  /**
   * Starts the timer for the animation.
   */
  void animate();
}
