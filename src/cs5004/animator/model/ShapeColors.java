package cs5004.animator.model;

/**
 * This Enum represents the different colors that a Shape can have. Currently, the only shape colors
 * are White, Black, Red, Green, Blue, Yellow, and Purple.
 */
public enum ShapeColors {
  WHITE, BLACK, RED, GREEN, BLUE, YELLOW, PURPLE,
}
